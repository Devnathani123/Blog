Flask==2.3.2
gunicorn==20.1.0
Jinja2==3.1.2  # for the render_template_string function
email-validator==1.3.0  # for handling email format validation (optional, but recommended)
itsdangerous==2.1.2  # required for Flask session management  # this is a built-in library, so it doesn't need to be listed
sqlite3  # this is also built into Python, so it doesn't need to be listed
